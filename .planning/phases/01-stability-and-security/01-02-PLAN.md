---
phase: 01-stability-and-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web-enhanced/requirements.txt
autonomous: true
requirements:
  - BACK-02

must_haves:
  truths:
    - "requirements.txt does not contain sse-starlette"
    - "requirements.txt pins fastapi >= 0.135.1"
  artifacts:
    - path: "web-enhanced/requirements.txt"
      provides: "Updated dependency pins"
      contains: "fastapi>=0.135.1"
  key_links: []
---

<objective>
Update requirements.txt to pin FastAPI >= 0.135.1 and remove sse-starlette (BACK-02).

Purpose: FastAPI >= 0.135.1 provides native `fastapi.sse.EventSourceResponse`, eliminating the need for the sse-starlette third-party dependency. This must happen before server.py can import from `fastapi.sse`.
Output: Updated requirements.txt with correct pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stability-and-security/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update requirements.txt — pin FastAPI, remove sse-starlette</name>
  <files>web-enhanced/requirements.txt</files>
  <read_first>
    - web-enhanced/requirements.txt
  </read_first>
  <action>
Replace the entire contents of `web-enhanced/requirements.txt` with:

```
fastapi>=0.135.1
uvicorn>=0.34.0
maigret>=0.6.0
```

This removes the `sse-starlette>=2.0.0` line (per BACK-02) and updates the FastAPI floor from `>=0.115.0` to `>=0.135.1` (minimum version shipping `fastapi.sse` module).

After writing the file, run:
```bash
cd /Users/sitticus/github/maigret/web-enhanced && pip install -r requirements.txt --upgrade
```

Then verify the installed FastAPI version supports the SSE module:
```bash
python -c "from fastapi.sse import EventSourceResponse; print('fastapi.sse import OK')"
```
  </action>
  <verify>
    <automated>cd /Users/sitticus/github/maigret && grep -v '^#' web-enhanced/requirements.txt | grep -c "sse-starlette"; test $? -ne 0 && python -c "from fastapi.sse import EventSourceResponse; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - web-enhanced/requirements.txt contains `fastapi>=0.135.1`
    - web-enhanced/requirements.txt does NOT contain `sse-starlette`
    - web-enhanced/requirements.txt contains exactly 3 non-empty lines (fastapi, uvicorn, maigret)
    - `python -c "from fastapi.sse import EventSourceResponse"` exits 0 (no ModuleNotFoundError)
  </acceptance_criteria>
  <done>requirements.txt has fastapi>=0.135.1, no sse-starlette line, and the installed FastAPI version can import fastapi.sse.EventSourceResponse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> local install | Third-party packages installed from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | requirements.txt dependency pins | accept | Floor pins (>=) used, standard PyPI install; package integrity verified by pip's hash checking. Local single-user tool — supply chain risk is low. |
</threat_model>

<verification>
- `grep "sse-starlette" web-enhanced/requirements.txt` returns no matches
- `grep "fastapi>=0.135.1" web-enhanced/requirements.txt` returns a match
- `python -c "from fastapi.sse import EventSourceResponse; print('OK')"` prints OK
</verification>

<success_criteria>
- sse-starlette removed from requirements.txt
- FastAPI pinned >= 0.135.1 and installed
- fastapi.sse module importable
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-and-security/01-02-SUMMARY.md`
</output>
